import React from "react";

const MarkFavouriteButton = ({
  userId,
  isFavourite,
}: {
  userId: string;
  isFavourite: boolean;
}) => {
  return <div>MarkFavouriteButton</div>;
};

export default MarkFavouriteButton;
